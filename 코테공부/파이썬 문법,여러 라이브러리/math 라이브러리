-최대 공약수, 최소 공배수 구할 때 유용함

import math

#최소 공배수(LCM)을 구하는 함수
def lcm(a,b):
    return a*b //math.gcd(a,b) # a*b를 최대공약수로 나눈 값을 리턴하도록 한 것
    #몫을 리턴하면 됨

a = 21
b = 14

print(math.gcd(21,14))
print(lcm(21,14))

최소 공